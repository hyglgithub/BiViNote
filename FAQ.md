# BiViNote 常见问题

## 登录状态异常解决方法

如果你已经登录 DeepSeek，但插件仍显示「未登录」，请先将 DeepSeek 标签页设置为**保持活动**，避免浏览器将页面置于 `sleeping` 状态。

**Edge：**
设置 → 系统和性能 → 性能 → 内存 →「使这些站点保持活动状态」→ 添加 `chat.deepseek.com`

**Chrome：**
设置 → 性能 →「始终让这些网站保持活动状态」→ 添加 `chat.deepseek.com`

添加后**刷新 DeepSeek 页面，再重新打开插件**即可。

> Edge 用户尤其建议添加该设置，避免后台 DeepSeek 页面进入休眠导致插件无法正常获取登录状态。
